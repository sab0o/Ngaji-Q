package com.example.ngajiq.ui.theme

import androidx.compose.ui.graphics.Color

// Base Colors (Your Input)
val IceBlue = Color(0xFFCEF0FF)     // very light blue
val SoftAzure = Color(0xFF68AFFF)   // medium soft blue
val PrimaryBlue = Color(0xFF5696F5) // bright blue
val DeepBlue = Color(0xFF297AF3)    // deeper saturated blue
val AquaBlue = Color(0xFF49B9DE)    // aqua accent
val BrightCyan = Color(0xFF55D5FF)  // new color

// Dark Theme Colors (Final)
val DarkPrimary = PrimaryBlue
val DarkOnPrimary = Color.Black

val DarkPrimaryContainer = DeepBlue
val DarkOnPrimaryContainer = IceBlue

val DarkSecondary = AquaBlue
val DarkOnSecondary = Color.Black

val DarkSecondaryContainer = SoftAzure
val DarkOnSecondaryContainer = Color.Black

val DarkTertiary = IceBlue
val DarkOnTertiary = Color.Black

val DarkBackground = Color(0xFF0D1117)   // near-black modern background
val DarkOnBackground = IceBlue

val DarkSurface = Color(0xFF12161C)
val DarkOnSurface = Color(0xFFE2F4FF)

val DarkSurfaceVariant = Color(0xFF1C232C)
val DarkOnSurfaceVariant = Color(0xFFBFD9F5)

val DarkOutline = Color(0xFF5D6A7B)
